{
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@schroedernet/commitlint-config": "^1.0.0"
  },
  "scripts": {
    "lint": "npx commitlint --config .config/commitlint.yml --from HEAD~1"
  }
}
